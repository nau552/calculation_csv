# 作業ルール（このリポジトリでの開発時に必ず従う）

## 変更完了の定義（Definition of Done）

コード変更（機能追加・仕様変更・バグ修正）は、**以下を済ませるまで「完了」と報告しない**。
ユーザーに指摘されてからやるのではなく、実装の一部として自発的に行う。

1. **テスト**: 新機能・変更のテストを追加し、`.venv/bin/python -m pytest`（Windows は
   `.venv/Scripts/`）全件パス。
2. **ドキュメント追随更新**（docs/README.md の索引にある運用どおり）:
   - `docs/code_reference.md` — 関数・モジュールの追加/変更をすべて反映（追随更新が運用）
   - `docs/testing_guide.md` — テストファイルの追加/変更を反映（同上）
   - `README.md` — ユーザーの使い方・config の書き方・ディレクトリ構成に影響する場合
   - `docs/score_gui_design.md` / `docs/score_gui_ui_design.md` — 設計判断を伴う場合
     （過去の記録は消さず、日付つき注記・追記で更新する）
   - `docs/score_gui_progress.md` — セッションの作業内容を追記（引き継ぎ用）
   - 進行中の仕様変更ノート（例: `docs/spec_change_dataname_measure.md`）があれば実装状況を反映
3. **バージョン判断**: `scorelib_param/__init__.py` の `__version__` を上げる必要が
   あるかを**毎回明示的に判断し、判断結果を報告に含める**（上げない場合も理由を言う）。
   - **版数はエンジン専用**。ui/ のみの変更では上げない（UI リリースは番号なしの
     `ui-YYYYMMDD` タグ。SVN と番号がズレて見える混乱を防ぐため — README 参照）
   - 設定ファイル（jsonc）の語彙・意味が変わる変更（新フィールド・新しい値の形）は
     **必ず上げる**: 旧エンジンで読めない設定が生まれるため、版で見分けられる必要がある
   - 目安は README「バージョンの上げ方」: config の意味に関わる変更=真ん中、
     それ以外の機能追加・修正=最後の数字
   - **CHANGELOG への記録はエンジン・UI どちらの変更でも必須**（エンジン変更は
     「未リリース」節へ、UI 変更は次の `ui-YYYYMMDD` 節の材料として）

## 進め方のルール（ユーザーが明示的に要求）

- 設計相談・バグ報告には、まず口頭で回答 → 実装方針を提示 → **同意を得てから実装**する。
  いきなりコードを書かない。
- 設計ノート・ドキュメントへの書き込みも同じ順序（回答 → 同意 → 書き込み）。
  疑義が出ている間は編集を保留する。
- 用語はユーザーの言葉に合わせる（自分が導入した用語を問い返されたら、注記で
  しのがず文書全体を置き換える）。
- UI 文言は簡潔優先: 肯定形1文+代替手段の導線まで。限定句や数式は docs へ。

## ブランチ・タグ・リリース（README「開発の進め方」「リリース手順」が正）

- main は常に「テスト全パス・いつでも SVN 同期できる」状態を保つ。
  数日がかりの機能は `feature/<名前>` ブランチで作業し、完成後に main へマージ
- タグは2系統: `ver.X.Y.Z` = エンジンリリース（`__version__` と一致・SVN 同期の
  単位）、`ui-YYYYMMDD` = UI のみのリリース（版数は動かさない）。
  `__version__` を上げたら CHANGELOG.md の「未リリース」を確定し、コミット後に
  `git tag -a ver.X.Y.Z` を打つ（SVN 同期はタグから行う）
- push 前テストは pre-push フック（scripts/hooks）と GitHub Actions が担保する

## 環境メモ

- コマンド表記は Ubuntu（`.venv/bin/`）が主。Windows マシンで作業するときは
  `.venv/Scripts/` に読み替える（README セットアップ参照）
- テスト: `.venv/bin/python -m pytest`（全件パスが完了条件）
- UI 起動: `.venv/bin/streamlit run ui/app.py`
- コミット・push・タグ付けはユーザーの指示があるときだけ行う
