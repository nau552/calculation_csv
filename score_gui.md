# 背景
- ベイズ最適化を用いたパラメータ最適化スクリプトを一般ユーザに展開するためのGUI化に取り組んでいる
- 最適化対象のモチーフを選び、最適化を実行する部分はすでにGUI化され、サーバ上で動作している。
- 最適化時のスコア計算は現在はpythonで記述されたファイルで計算しているが、これもGUI化して一般ユーザでも自由にスコア設計ができるようにすることが本プロジェクトの目的である。
- 最初はすでに動作しているGUIに影響を及ぼさないため、まずはPhase1として、完全に独立したものとして作りたいと考えている。
- 1epoch分の測定結果を出力したcsvファイルの一部は例としてresult_tmpにおいてある。
# 一般ユーザでも設計できるスコアについて
- スコアとはスコアパーツの合成（加減乗除やlog等の演算による組み合わせ）によって構成される。
- スコアパーツとは、データフレーム形式で読めるように出力されたcsvファイルについて、各軸をどのような集計をするか指示することで計算する。
- スコアパーツとは、一つのcsvファイルから作成されるもので、FBC.csvではFail bit cout、tPROG.csvでは書き込み時間に関する出力データが保存されている。
- 集計指示とは例えばWLという軸で最大値を取り、STRという軸では0から5までの値のうち0,1,2の平均値を取るなど、他にもRead_Label軸がupper1になっているState軸のR2Aの値のみ抽出する、Program_Override軸のFalseを分母、Trueを分子とした相対値を算出する、など多岐にわたるものがある。
- WLはユーザが必要とする場合、Group単位で分割して、例えばWL0~3がWLgroup0, WL4~5がWLgroup1,...というようにユーザが自由に分割定義を設定し、その分割単位で、worstのWLgroupといったような指標でも集計できるようになっている。
- 各軸の集計方法を指定した順番通りに集計することで各スコアパーツの値が一意に定まる。
- dVtBudgetという指標があり、これはFBCの相対値からStateごとに特定の変換係数をかけることで算出される。
- 実験に用いるチップの世代に応じて各Stateの変換係数は変わるため、このような変換係数や、先述したWLgroupも定義を記述したファイルがあり、そこから読み取って使用する。
- こうして作ったスコアパーツを自由に組み合わせてスコアを作ったり、満たすべき制約値（その値が10以上の場合は解として選ばないなど）として制約設定などもできるものとする。
# 現在のスコア計算方法
- result_tmp以下にあるのは出力データのうちFBCに関連するものだけを一例として置いてある。
- pythonで記述しているスコアでは、dataName_FBC, map_dataName, map_State, map_Label, map_Override, parameterLabel_FBCから、FBC.csvのmeasure列を解決することで、FBC.csvを読んだデータフレームを(Erase/Program/Read)_(Label_Override)列を作成する形で作る。展開結果の例はgomi\FBC_expanded.csvにある。
- こうすることで、所望のRead_Labelの内容やOverrideの有無を人がわかる形とできるため、このデータフレーム処理することで様々なスコアを実装している。
# Phase1のスコア設計用GUIの仕様
- 独立している、ということについて以下で定義する。
    - まずはスコア設計用GUIをサーバ上では動作させず、ユーザのローカルでstreamlitなりReactなりの形でスコア設計用ページを立ててもらう。
    - そして、スコア設計用ページでスコアを作成したら完了ボタンを押すことで設計されたスコアをテキストファイル等の形式でユーザのローカルに保存する。
    - ユーザは保存したファイルを現行のGUIにアップロードすることで、最適化実行する際の適切なディレクトリに転送することでそのスコアで最適化の各epochの結果が計算される。
    - スコアの定義等に必要なファイルは現行GUIからダウンロードボタンを押すことでローカルに保存し、ユーザがそれをアップロードすることでスコア設計用GUIは情報を取得する。
- GUI画面では、プラスボタン等でスコアパーツを追加し、その名前を自由に入力する。
- 次にどのcsvをベースにするかを選択するため、typeについて選択する。FBCについて計算したい場合はtypeをFBC、tPROGについて計算したいならtypeをtPROGと選択する。
- 選ばれたtypeに合わせてそのデータで集計指示できうる軸を表示し、各軸について集計指示を記入する。記入方法としては、一般的なものやその軸がとりうる値の範囲等を選択でき、自由記述もできるとする。
- 先述したようにcsvに存在する軸以外に、相対値を取るような計算もできなくてはならない。これは、FBCの絶対値では測定に使うチップ等の個体差が出てしまうため、毎epochで基準パラの測定結果と、ベイズが提案してきたパラの測定結果が入っていることから、基準を分母、提案を分子とした相対値としてスコアパーツを作ることが一般的だからである。
- この分母を取る際に、分母だけをある軸で何らかの集計ができることも必要となる。例えばworstをどう扱うか等を考慮するため、WL,STRの2軸で平均を取ったものを分母とするような集計指示を出せる必要がある。
- dVtBudgetを計算するときはtypeをdVtBudgetとすることで、FBC.csvを読むものの、自動で相対値を取り、対応する世代の変換係数をかけることでdVtBudget変換をするものとする。
- WLgroup等の分割や、dVtBudgetの変換係数も、どこかから読み取るか、ポップアップ的に選択、編集できると良い。
- このようにして相対化の有無や各軸の集計指示をどのような順番で集計するかも設定できるものとする。これは例えばOrderというような設定欄があり、集計指示がある各軸や（行う場合は）相対化指示を自由な順番で選べるようなイメージをしているが、わかりやすく順番が指定できればこれにこだわらない。
- スコアパーツは以前使ったものを何らかのサーバ等から読み込むこともできるものとする。
- 作成したスコアはダミーデータを用いることで、エラーなく動作するか確認するものとする。
- このテストの際、その実験ごとにどのような出力となるかは変わるはずであり、result_tmpにあるような出力ファイルは実験前には存在しないため、測定に使用するデータファイルから出力されるデータを予測する必要がある。
# GUI化に伴いバックエンドで必要となるスコア、スコアパーツの計算方法
- テキストで受け取った各スコアパーツの集計指示を解析することで、指示通りの集計を行う。
- 現在の最適化スクリプトはminiforgeを用いた仮想環境上のpython3.7で動作しており大変古いため、スコア計算部分はminiforgeで別の仮想環境としてpython3.13などを使用し、サブプロセスで起動し、高速で計算させるものとする。
- この計算はpolarsを用いて、lazy, stream, scan, perquet等を必要に応じて適切な形で使用し、メモリ効率、計算速度ともに最良の形で実装するものとする。

# 実際に実装すべき内容と指示
- ユーザにローカルで建ててもらうstreamlit等で作成するUI
- UIで設計されたスコアを適切な形（スコア計算する際に解釈しやすい形）でファイル出力する機能。
- 実際にスコア計算を行うバックエンド部分

- これらのうち、まずはどこから実装すべきか？
- また、詳細設計をまだ固めるべき点がある場合は適切な形で質問せよ。
- テスト可能な形で実装せよ。
- 実装を行う前に設計書を適切に作成せよ。

確認事項に答えると同時に疑問点について確認したい。
## 2. 全体アーキテクチャ
について、よくわからない。
現行GUIから、定義ファイル(WLgroup, dVtBudget係数)やパラメータラベル等のスコア生成に必要なファイルをダウンロードし、それをユーザがStreamlit UIに読み込ませることでスコア設計をして、設計したスコアを保存。現行GUIへアップロードするようなイメージ。
Streamlit UIで入力された情報をjsoncに変換することにはpolars等は使わないと思っていたが使うのか？

1. map系ファイルはtype間共有か、type別に分かれうるか（3.1節）
について、map系ファイルは共通。

### 3.2 軸（Axis）の分類
について、現行のスコア計算はpythonで直接記述する形であることから、人がわかりやすくするためにFBC.csvなどの{type}.csvを変換して用いているが、今回実装する形ではわざわざ一旦この形に整形しなおさず、ユーザの指示に合わせて必要な情報を抽出すれば良いかと思ったがどうか？これは難しいか？

2. DataNameの`reference`/`evaluation`命名規則は全type共通の固定パターンか（3.3節）
について、どちらかというとOverrideがTrueかどうかがevaluationかどうかの判断基準になると考えている。
例示しているresult_tmpは読み込み系最適化の例のため、Read_Overrideの部分でOverride=True, Falseの判断をすることを考えているが、これが書き込み系のときはProgram_Overrideの部分が変わるのではないかと想像している。
ここについては担当者に確認中。

3. dVtBudgetは世代×温度×Stateの3キーでよいか。温度はGUI上でユーザが選択する
   グローバルパラメータという理解でよいか（3.5節 確認事項1,2,3）
について、確認事項1：温度は測定結果とともに帰ってくる。result_tmp\initial_temperature.csvに例のファイルを用意した。このファイルの0,1はBoard列を意味しており、一般的には高温Boardと低温Boardの2Boardで測定することが多い。このファイルに記載されている温度により近い係数が使用される。
**[確認事項2]** 変換式は `dVtBudget = a * relative_FBC + b` という線形式という理解でよいか。
について、変換式は整理するとaは使わず、-log10(relative_FBC)/b *1000となる。
また、これはdVtBudget計算に関わらず、相対化をするときは分母が0であると困ることからoffset値として1や20を足すことを前提とする。
**[確認事項3]**について、正式にjsonc化して良い。

## 4. スコアパーツ（ScorePart）仕様
### 4.1 全体構造（提案）
について、typeがFBCなのにdvtbudget要素があるのはどういうこと？
// type = "dVtBudget" のときのみ
とコメントがあるが、この例がFBCなのならこの項目があるのはおかしいのでは？
また、generationは計算時にconfigファイルから取得できる。温度は先述の通りなので、この項目は必要ないはず。

4. `worst`集計の方向（max/minどちらが厳しいか）は軸・パーツごとに指定させてよいか（4.1節）
について、この最適化では数値が小さい方がより良いという前提に立っている。worstというopはなくても、最も良いものを取りたいときはminを選ぶし悪いものを取りたいときはmaxを選ぶからこれもいらないと思うが、必要性はあるか？

5. 自由記述式（expr）の実装方針：独自ミニDSL vs `simpleeval`等の既存ライブラリ（4.3節）
について、これはどちらが良いかわからない。一般的な演算は基本的にできるようにしておきたい。

## 5. スコア（Score）仕様
について、Scoreには名前をつけなくても、
"0.5 * FBC_A2B_upper1_rel + 0.3 * log(FBC_C2D_upper1_rel) - dVtBudget_R2A"のような記述だけあれば良いと思うがどうか？つまりスコアパーツとともに同じファイルにまとめてよいと思う。
制約条件に関しては、最適化内部で自動的にその指標がここで指定された値よりも大きい（＝悪ければ）ならばNGという動作をするため、opは使えないかも。
また、制約には動的制約という機能があり、その機能がオンになった場合は、最適化の過程でそのepochまでで算出されたその指標の測定値から計算される動的値を制約とする場合がある。これは、例えばその時点での20パーセンタイル値と、設定に記述されている値の大きい方を選ぶというように使われる。つまり、最初は指定値よりも動的値は大きく、最適化が進むにつれて指定値に近づきながら、指定値未満にはならない、というような動作をイメージしている。

6. 制約の`action`はPhase1では`reject`のみでよいか（5節）
について、上記の通り、rejectのような解として認めない動作は最適化の内部動作で行われるためここで動作を指定するものではない。

7. jsoncの命名規則・スタイルを現行の他設定ファイルに揃えたい場合、サンプル共有は可能か（6節）
について、sample.jsoncを更新する形でサンプル共有した。

## 7. 計算エンジン
について、
subprocessで呼ぶのはPhase2ではない。
このツールで作成したスコアを使った最適化実験は3.7で動作するため、このスコアを計算する部分はサブプロセスで動作する形で作らなければならない。

## 8. ダミーデータによるテスト
について、
`parameterLabel_{type}.csv`などのresult_tmpにあるファイルは測定前には存在しないためこれらから軸候補は取得できない。
様々なファイルをGUIから取得する必要があり、これについては改めて整備してから支持するため、きちんとしたテストの実装はあとで良い。


1. 現行の最適化スクリプト(python3.7)側に、新しいsubprocessエンジンを呼ぶ口が既にあるか、こちらでブリッジ部分も作る必要があるか
について、テストで作ったことはあるが現行スクリプトには入っていない。そのepochでの測定結果はすべてresult_tmpに入っており、計算に使うオプションはjsoncから読み取ったconfig変数に入っており、指示はturbo.pyの中でget_score()という関数が呼ばれ、その中でresult_tmpから必要なcsvファイルを読み取りconfigを見ながら計算する形なので、configを受け取り、計算結果のデータフレームを返すことさえできれば良いため、サブプロセスエンジンも独立でここで作ってしまってそれを差し込むだけで良いのではないかと考えている。
2. score.jsonc が現行GUIの optimization.score_function 等とどう紐づくか（配置ルール・参照名）
について、score_functionは、score.pyの関数名を指定することでその関数が実行される形を取っている。そこで、例えば"gui_score"というような指定がされたらget_score関数で分岐して、今回作るエンジンが呼ばれてそっちで計算されるようなイメージをしていた。score.jsoncの情報はconfigのoptimization{}の中に入れば良いのではないかと考えているが、より良いものがあるかはわからない。



標準出力をパースしてDataFrame化する想定（ブリッジは現行スクリプト側整備後に実装）。
→DFを直接返してほしいが

WL19までしかないのでは？

また、スコアパーツは10数個あることが多い。また、この10数個のスコアパーツの半分以上はFBC.csvから算出するものが多い。
このとき、今のように順番に最初からスコアパーツを一つずつ計算しているとかなりの時間がかかる気がする。
現行のスコア計算は以前も説明したようにpythonファイルで直接記述していることから、例えばStateが違うだけのdVtBudgetなんかを使うときは、先に全State分まとめて計算しておいて、必要なStateを選択して指標として使うなどができる。
また、各ファイルについても最初に一度読んでおけばそれを使いまわすこともできるから読み込みのタイムロスもない。
このようなことを考えたとき、現在実装しているスコア計算エンジンは、何らかの工夫でもって高速化することは可能だろうか？
実際の実装はまだしなくて良いので、案があれば提案してほしい。

perquetやstreamingについてよく理解はしていないが、今の規模では不要とのことなので追加のコメント。
今は1epochごとに実測した結果から計算してスコアを算出することを想定しているが、実は今後実装する予定の新機能で過去データ活用というものがある。これは、過去に行った同系統の実験データをベイズ最適化のモデル構築の際のベースに組み込むことで、実測数を減らしながら精度を向上させようというものである。
この過去データ活用は場合によっては数千epoch分読み込む必要がでることもあるため、一つのスコアパーツを計算するときに数十～数百epoch分のcsv（FBC.csvなど）を読み込み、epoch列を付与するなどしてバッチ処理することで、一度の計算で複数epochのスコアパーツ計算をまとめて行うことを考えている。
このようなときにはperquetやstreamingが必要になるのではないかと考えている。
これも含めて現在の高速化も見据えたとき、どのような実装にすべき？

では
1. 今やる: 案A（type共有読み込み）+案B（前段キャッシュ）。同時に collapse_to_scalar を「識別軸を残せる」形に一般化しておく。
を実際に進めたい。現在の計算結果が変わってしまうことは本当にないか？
案A,Bの具体例がいまいちわかっていないが、configでの書き方が変わったり何か注意する点はない？

案A: SharedComputeContext がtype別に必要軸の和集合で1回だけ読み込み、各パーツには必要列だけを射影して渡します。この射影が正しさの要で、余分な列が残ると相対化のペア照合キーが変わってしまうため、テストで「共有あり=共有なし」の等価性を全パーツについて検証しています。
の意味がいまいちわかっていない。
必要軸の和集合とは？各パーツには必要列だけを射影とは？ペア照合キーとは？具体例で説明してほしい。

案B: __relative__/__dvtbudget__ 直後の中間結果を「type+必要軸集合+そこまでの全ステップ内容」をキーにキャッシュ。
について、具体的にどこでどうやってキャッシュしてる？

スコアパーツの計算においては、相対化など以外にも、OverrideのTrueからFalseの値を引くことでdelta値を取ることもある。
こういうものも実現できるか？
また、あるStateのdVtBudgetと別のdVtBudgetの和や差を取ったものをスコアパーツとしたい場合もある。
こういうこともできる？
つまり、例えば後者についてはState列の集計指示の際、普段はfilterで一つのStateを選んでいるが、二つのStateの差を取る指示を出すようなイメージ。


relativeのenableって必要？書いたけどFalseにしたいことを想定している？書いてあれば自動でTrueではいけない？
sumはsubsetなのにdiffはdiff？diff_subsetとするか、どっちもsubsetとかつけずに、valueがついたらこれまでのsubsetと同義になるのとどっちが良い？ほかに良い方法があればそれでも。

Stateを二つえらんでdiffを取る件、それぞれ異なるRead_Labelを使いたいこともある。
これは一つのスコアパーツで記述するのは難しいか？
実はdVtBudgetを計算するときも、Stateごとの和を取ることがあるのだが、StateがR2A,A2Bのように上方向へのBudgetと、A2R, B2Aのように下方向へのBudgetを見る際、それぞれ上方向と下方向を検出しやすいRead_Lavelを設定していることが多い。
このことから、R2A,A2Bはread_level_upper1, A2R,B2Aはread_level_lower1の方を取り、これらの和を取ったりしたものをスコアパーツとして、制約条件に用いることがある。これを実現するにはどうしたら良い？

"value": [
    {"State": "R2A", "Read_Label": "read_level_upper1"},
    {"State": "A2R", "Read_Label": "read_level_lower1"}
]
複合軸のときはこんな風に書くことにして、keyはどんなopでもvalueにしてはいけない？
自分が気になっているのは、UIが導入されるまでの間で、ユーザがvaluesのところを中身は正しいのにkeyだけvalueと書いてしまったせいでエラーになったり計算が間違ったりすると、このopはvalues、このopはvalueという風に覚えないといけないのが面倒なんじゃないかと思った。
これに問題があれば教えてほしい。


複合軸集計で
        "State&Read_Label": {
            "op": "sum",
            "value": [
                {"State": "R2A", "Read_Label": "read_level_upper1"},   // 上方向はupper1で
                {"State": "A2B", "Read_Label": "read_level_upper1"},
                {"State": "A2R", "Read_Label": "read_level_lower1"},   // 下方向はlower1で
                {"State": "B2A", "Read_Label": "read_level_lower1"}
            ]
        },
このように書いたとして、これを使いまわしたいこともあると思う。
そのときはこれを毎回そのパーツにコピペするしかない？
configを手で修正しているときは面倒ではあるが最悪コピペしても良いかもしれないが、
GUI化した後なんかは、パーツごとにこの複合軸の設定を毎回ひとつずつ記入なり選択しないといけない？
他に良い方法はある？
すぐに実装は開始せず、良い案があれば言葉で提案せよ。


### 画面2: スコアパーツ編集
について、スコアパーツを新規作成することについて書いていないように見えるが大丈夫か？


最初のデータ読み込みについて、result_tmpディレクトリには通常測定結果のみが入るもので、optimization設定jsoncやdVtBudget係数jsoncは存在しない。
例示するresult_tmp相当のディレクトリにはこれらのファイルを置いておく必要があるということ？
また、空文字列のまま読み込みボタンを押すと一通り見つかったような表記が出てきたがこれはなに？

- ~/.scorelib_draft.jsonc へ自動保存されるとあるがこれはどのタイミング？
- 何らかの操作をしたときにやり直したいことがあると思う。戻るボタンのようなものは作れる？
- 相対化の事前集計はopの対象を細かく選んだりはできない？
- 事前集計で選んだ軸が下のOrderにあるとバグらない？
- Read_OverrideがOrderにない。relativeにいるから？そうだとして、relativeのチェック外して、相対化しないことにしても復活しない。
- 集計指示のところ、meanからmaxとかに変更しても最初見た目変わらず、上とか下ボタン押したら変わる。これは内部ではちゃんと変わっている？どちらにしても選択したときにちゃんと表示も変わってほしい。
- 複合軸のときの値選択で行追加したり何らかの操作をするとそのプルダウンみたいなものが一旦閉じられて操作しづらい。開いたままで行追加は難しい？
- 変換ステップ名ってなに？自由に入力できるけど名前に意味はない？addしか操作がなく見える。

- スコア合成・制約のところ、スコアパーツを選択しても内部的には多分増えていて、サイドバーでページを切り替えると増えてるようだが、expression入力画面がすぐには変わらない。
- スコアパーツも順番を入れ替えられるようにしたいが難しい？

- 前回からの内容を使うかと聞かれるが、使うと押してもスコアパーツ編集のところで「先に画面1でデータを読み込んでください」と表示されている。てっきりこれでスコアパーツとかも復元できるのかと思っていたが違う？
- ドラッグドロップについて、てっきりよくUIとして見る三本線みたいなものが横に出て、入れ替えることができるのかと思ったが、一旦入れ替え画面に切り替えないといけない？これだと、入れ替えるために一旦切り替えて、入れ替えたあとにチェックを外すことで画面を戻してまた集計指示を編集するという感じになっているが、これは仕方ない？


前回の内容を使ったとき、optimization設定jsonc のパス（任意）、dVtBudget係数jsonc のパス（任意）の二つが空欄になってるが大丈夫？
ドラッグドロップの入れ替えについて、やっぱり望みと違う。てっきり入れ替え欄のところがプルダウンで開けるのかと思ってた。そのような形は難しい？難しければ仕方ないが、ユーザとして使いやすい形とはどんなものだろうか。現時点で強いて言うなら入れ替えのところが赤いのがまぶしくて目が痛い。


- "あなたの回答には今の常時表示には「同じ一覧が2回出ていて画面がうるさい」という欠点"という表記と"普段の編集は「編集するエントリ」プルダウン + 編集欄（今のまま）"という表記があるが、今の実装を正しく理解できている？今は入れ替え欄常時表示、軸選択はプルダウンで選ぶことで常時表示されている編集中の画面が選択した軸用の画面に変わるのでは？正しく理解できているかを再度確認したうえで、ユーザにとって使いやすいものを提案せよ。



- ドラッグドロップの欄について、各項目の色が通常の枠と同じ色のため、何となく見づらい。軸やパーツ名の項目の四角の色を少し色を変えたりして視認性を上げたりできる？
- また、文字が中央ぞろえのため、移動のマークはせっかくついているのに横位置がそろってなくて操作しづらそう。移動マークだけ左にそろえるか、文字全体を左にそろえるかなど、何か操作しやすくなる方法ある？
- budget変換という軸を追加できるのであれば、type=FBCでdVtBudget変換用軸追加type=dVtBudgetというのはいらなくなったりする？
そうすればtypeは存在するcsvファイルのみ選択する形になる？dVtBudget用typeを残しておいた方が良いメリットがありそうであれば教えて。
- 少し今更だが、group分割後の集計と、集計前の集計はタイミングを変えたいことが多い。例えばWLgroup分割をした上でWL,STR,Block方向に平均を取り、Board方向にmaxを取ったあとにWLgroupのmaxを取ったりしたいことがある。集計タイミングをずらすのは難しい？
- また、WLgroup分割の定義は編集できるようにできない？

- group_reduce を残す「互換のため」という理由がよくわからない。また、頻度は低いがSTRなどもgroup分割したいことがある。つまりサンプルとして読み込んだ分割以外にも分割を増やしたいこともある。ファイル出力の時にWL分割や追加した分割もまとめて出力する？どうするのが良いだろうか。

WLgroup は、最適化側でパラメータ割り当ての単位としても使われているとはどういう意味？作成したconfigファイルをスコア以外の内容も記述した設定と合成して最適化実験スクリプトを起動し、最初にこの設定ファイルを読んで最適化が開始するため、最適化時もこのgroupが使われるが、あなたの言っている懸念を一応説明して。

- UI上のエラーメッセージがわからない。score_parts.12.aggregations.WL: op 'group_reduce' has been removed; define the group in groupDefs and put its name (e.g. 'WLgroup') in order as a derived axis instead (inner op on the source axis, outer op on the group axis)とあるが、score_parts.12とは？おかしいスコアパーツ名がわかると嬉しいし、スコアパーツ一覧のところで色を変えたりエラーがあるとわかるようになると良いかも。
- WLの数がgroupと合わないときってどうなるの？例えばresult_tmpにあるFBC.csvはWLが0から5までしかないが、例として読み込むconfigには0から20までの定義が書いてあったりする。これでもエラーなく計算されているようだが、どういう動作をしている？

- WLgroupの範囲について、一般的に範囲と本数が合わないことは異常なため、エラーを出したいが、現状では本番測定で得られる本数がわからないため難しいんだと思う。
- おそらく現状でそのあたりの情報をまとめており、現行のスクリプトでも使用されているであろうファイル群から一つの例としてtests\fixtures\B9LS.jsonを作成した。
- これは本来generationごとに存在するもので、本来のB9LSとは内容が異なるが、このテスト用に一つだけ作成した。このファイルもUIで読み込めるようにしてほしい。
- ここにはSTRの数も記されているため、読み込んだconfigにWLやSTRの既存group分けがもしあった場合、その値がWLの数やSTRの数とあっていなければ事前に警告を出すようにしてほしい。

- group定義の警告がリアルタイムじゃない気がする。
- また、意味のわからない現象が起こっている。例えばdAR_marginというパーツを複製するとdAR_margin_1というパーツが作られ、このパーツが選択された状態になると思うが、この後に元のdAR_marginを選択しなおすとどちらも名前がdAR_margin_1になって名前が重複しているとか言われる。
- しかも、dAR_marginの方ではrelative設定をしているときにこれを複製し、dAR_margin_1の方ではrelativeをオフにしたら、dAR_marginの方でエラーが出て、その内容が、"相対化指示がOrderにあるのにその内容がない"というような表示がされ、相対化チェックがはずれている。

- 間違えて名前を同じに設定してしまったときにおかしくなる気がする。
- 例えばdAR_marginというパーツがあり、別のパーツでdAR_margin_2というものがあったとき、こちらをdAR_marginと変えると、まず重複の警告がでる。ここまでは良いが、そのあと、元のdAR_marginを選択しても選択されず、一旦別のパーツを選択した後にプルダウンで、元のdAR_marginを選んでもあとから重複させたdAR_marginが選ばれるような状態になっている気がする。

- pythonの知識がある人が、例えば複数のcsvファイル等を使って複雑な集計を行いたいときに、pythonで一つのスコアパーツを計算する関数を作成し、それをスコアパーツとしてconfigに名前を書くような機能は追加できる？
- つまり、ユーザが自由に関数定義できる用のファイルを用意し、ユーザはそこで好きな名前で関数を作成し、そこにはスコアパーツと同様に一つの値を返すものとする。
- ここで作った関数を、configに直接記入したり、UI上で選べるようにしたい。
- configに直接記入する場合、例として、score_partsとして関数名をnameに設定し、typeをその専用ファイルから使うことを明示する文字列で設定すると、普段のエンジンではなく、そちらのファイル内の指定の関数が呼ばれるイメージ。UI上でも同様とする。
- 私が例として挙げた方法以外にも良い方法があればそれでも良い。まずは実装可否と、その方針を示してみよ。
- これとは別件に近いが、README.mdに"全軸を潰し切るとパーツの値が1スカラーに定まる。潰し残しがあるとエラーになる（テスト機能を兼ねる）。InBatchEpochは実質未使用（常に0）のため通常orderに含めなくてよい。InBatchEpochは実質未使用（常に0）のため通常orderに含めなくてよい。"との記述があるが、InBatchEpoch列はどう処理されている？


- custom_parts.pyの用意の仕方は悩ましいところがあるので相談したい。
- まず整理すると、現行GUIが起動しているサーバがあり、一般ユーザはブラウザからそのサーバにアクセスする形でGUIを使う。
- このとき、GUI上で、SVNに登録されている好きなリビジョンをダウンロードするボタンを押すことで、サーバの起動しているマシン上にその実験専用のディレクトリが作成される。
- それ以降もGUIで実験の様々な設定を行い、実験開始ボタンを押すことでGUIの起動しているマシン上で実験が開始され、測定結果をスコア計算する形。
- ここで、今回作っているUIは、ユーザ自身、もしくは何らかのサーバでstreamlitを起動することで使用することを考えている。少なくとも最終的にはGUIと連携させたり、GUIが起動しているサーバと同じサーバで起動させることを考えている。
- スコアの設計のために必要なファイルは、この現行GUI側から一旦ユーザのローカルからダウンロードして、streamlitUIにアップロードする形を取りたい。
- 本題とそれるがこうなると、そもそも現在のUIで複数のファイルをパス指定で行っているが、今後ユーザがUIを起動しないことを考えると最初からアップロード形式にしておいた方が良い？
- この場合、複数ファイルをアップロードするのはめんどくさいので、必要ファイル群はzip等でダウンロードしたり、それをzipファイル等のままUIにアップロードする形の方が良い？
- 本題に戻るが、実際の最適化はユーザのマシンではないところで行われることから、今作っているエンジンもSVNに登録する必要があることは理解していると思う。
- このとき、custom用の関数もそちらにないと計算ができないのではないだろうか。
- 基本的には一般ユーザはpython知識がないため、カスタムを使う想定はしていないが、仮にユーザが自分で指定した関数を使いたいとしても、サーバ側でダウンロードしたSVNリポジトリで最適化が行われることを考えたら、カスタム関数を編集できるのはSVN側だけでないといけないか？
- つまり、エンジン側にいる必要がある？その場合、UI上でカスタム関数を使うためにはどうしたら良い？GUI側からの必要ファイルダウンロード時にこれもダウンロードする？

- zipをアップロードするとき、result_tmpの中身を展開して他のconfigと同様に1階層にしていないとファイルが見つけられていないようだ。
- 別に展開して保存するようにしてもらっても良いが、zip展開後のディレクトリの中にディレクトリがあっても見つけられる方が良いのではないか。こうするデメリットがあれば教えて。

- 今更だが、さっきも言ったようにUIはユーザ個人か、どこか別のところで建てたり、最終的にはGUIサーバと同じところで建てることが想定されている。
- この時、UI上でテストをするためにはエンジンが必要だと思うが、その場合エンジンはどこに存在することが想定されている？
- このエンジンがメインで動作するのは当然最適化時なので、SVNに登録するのが普通だと思う。
- その場合、例えばユーザにUIを立てさせた状態でUI上でテストするにはどうする？エンジンをGUIからダウンロードする？
- 逆に言うと、テストをする気がない場合はエンジンを必要としないわけだが、現在ui用のファイルとエンジンには少なくともimport関連の依存関係があると思う。
- GUIからダウンロードしたとしてもimportの依存があるということはダウンロードしたものを適切なディレクトリに配置しなければならない？
- ユーザにstreamlitを立てさせる場合、python環境を作らせなければならないなどの弊害があることから、最初から別サーバで建てる可能性はあるが、エンジンとuiの依存は解決しなければならない？
- それともGUIからSVNからダウンロードしてきたエンジンをzipでまとめてアップロードすればそれをちゃんと使える？

- ちょっと待った。さっきから言っているように一般ユーザはGUI上でSVNからスクリプトを操作するため、ユーザ自身はSVNからチェックアウトするようなことはできない。
- つまり、もしユーザにStreamlitサーバを自身で建ててもらう場合はuiスクリプト群を何らかの形で渡すことを想定していた。
- ui自身もSVNに登録することは考えておらず、現行GUIもSVNには入っておらずgitで管理されているはず。

- その問題を考えると、最初の段階からユーザ自身にstreamlit環境を建ててもらう想定から離れた方がより良いか？
- また、先ほどテスト関係なく依存していると言っていたが、エンジンは与えられたconfig情報から計算だけをする、uiはそのconfigを作成する、という観点から、独立にすることは難しい？難しい場合は何が難しい？


- すべてのファイルをあなたに作成してもらったため、チーム内での報告等でもスクリプトがどうなっているか説明できない状態。各pythonファイルにどんな関数がありどんな内容なのか、テストも含めて詳細に説明してほしいが、その前に、やってほしいことがある。
- 各スクリプト内で意味のないものや重複など、修正を繰り返しながらAIが作成したコードにありがちなぐちゃぐちゃで可読性も低い状態だったり適切なコメントがついていない状態がないかリファクタリングせよ。仮に修正が必要な場合、その修正は同意を得てから行うこと。
- 普段テストを作らないため、いまいち使い方や考え方がわかっていない。ユニットテスト？単体テスト？それ以外にも結合テストとかがある？とかも意味がよくわかってない。テストについても説明して。
- 上記の説明を適切なファイルにまとめよ。欲しいのはリファクタリングを終えた後の現在の最終系のスクリプトの説明。
- ここでいう適切なファイルとは、既存のファイルでも新しいファイルでも良いが、そうする理由を説明して同意を得てからファイル出力せよ。


- gomiとかもテストで参照してるの？csvみたいな大規模ファイルってgitとかに登録しない方が良いのかと思って.gitignoreに入れていたが、テストに使うなら登録すべき？
- ここにユーザ向けの説明とかも書く、つまりユーザもここを覗く前提があるなら、gomiだとかresult_tmp_miniみたいな変な名前のディレクトリが表に見えてるのは微妙かと思った。
- 同様に、設計書関連が直下に置いてあるのもこれで良いのか？良くdocsとかに入っているのを見るが、これはそうしない？
- 自分の勉強用のtesting_guide.mdとかと、人に見せるファイルの区別はどう扱うべき？

- docstringやコメントは日本語にするのは難しい？

- 今日はここまでにしようと思う。タスクが変わってもここまでの現状を理解できるように、残っている課題も含めてここまでの流れをまとめてscore_gui_progress.mdを更新してほしい。すでに他ファイル更新の際に適宜更新されてる？


- あなたにはこのディレクトリでパラメータ最適化実験のスコア計算エンジンと、StreamlitUIを作ってもらっていた。
- 新機能が作れるか、もしくはもうほぼ対応できるかを確認したい。
- 作りたい新機能は、過去におこなった実験を、ベイズ最適化を用いたパラメータ最適化実験の初期モデル作成のベースにするため、生データを読み込んでモデルを計算する機能である。
- result_tmp\FBC.csvのようなcsvファイルが1epoch分のデータの1typeに属するものに当たるため、このようなファイルを大量に読んでスコア計算する必要がある。
- ここで、仮に通常の最適化のように、あなたに作ってもらったscorelib以下のエンジンを普通に使って1epoch分ずつを計算していては大きく時間がかかってしまう。
- そのため、大量のデータをバッチ単位でスコア計算して、そのバッチ内のepochの個数分だけのスコアを返す機能が欲しい。
- 現行のエンジンや、UIとの依存を壊さずにバッチ処理のスコア計算を実現できる？できる場合はどのように実現する？
- 例えば50~100epoch分のデータをまとめて処理するとすると、100万行以上のデータをそのepoch分だけまとめて計算することになるため、メモリ効率や計算速度など、特殊な処理が必要になると思う。
- これまでのドキュメントはREADME.mdと、docs以下のmdファイルにある。（score_gui.mdは私があなたと会話するためのメモに使ったものなので無視してもよい）
- これらのドキュメントやスクリプトを適切に内容理解した上で、現在実現できている状態を壊さずに新機能実現できるかを確認せよ。

- 自動発見はしなくてよい。そもそもスコアとは、最適化モデルが提案したパラメータで測定した結果のデータに対するスコアである。
- 最適化対象モチーフごとに対象パラメータも異なることから、同一のパラメータでの過去実験しか基本的には活用できない。
- そのため、大量に蓄積されているデータサーバから使えるデータを選出する機能が必要となる。
- この選出機能については今のバッチ処理とは無関係なため、別で依頼することとする。
- つまり、スコア計算エンジン側は、使えると判断されたデータを受け取るという前提。（もちろん判断漏れがあり得るため適切なエラー処理は必要）
- 本題の過去実験epochディレクトリについては、ある実験ログディレクトリの中にresult_history/result.0001というような構造で、このresult.0001の中に1epoch目のデータ(result_tmpにあたるもの)が格納されている。このディレクトリの中のcsvはtar.gz等に圧縮されている場合があるため、適切に解凍が必要。
- また、複数の過去実験のresult_historyを活用することが通常なため、0001にあたるものが複数存在することに留意が必要。
- この前提で、複数のresult_historyへのパスが渡されるものと現在は想定している。
- 場合によって、一度に大量の活用用データを最適化実験を行うサーバに送ることが難しい可能性もあるため、バッチ単位でデータダウンロード、バッチ計算、ダウンロードしたデータを削除、新たなデータをダウンロード、という流れが必要になる可能性が高いため、これも設計時考慮する必要がある。
- また、バッチ計算でマシンスペックにより、仮に時間がかかることがある場合、バッチ単位でダウンロード、バッチ計算、計算中に裏で次以降のバッチのデータをいくつかダウンロード、計算が終わり次第次のダウンロード済みデータをバッチ計算、計算中に裏で計算済みのバッチデータを削除、次のバッチデータをダウンロード、サーバの容量も考慮し、最大でも数バッチ分だけを先行ダウンロードとする、というような流れも想定する必要がある。
- 識別軸はEpochで良いと思う。バッチ提案が今後行われるかは未確定。
- ここまでの説明を理解し、設計方針をマークダウンにまとめた上で同意を得てから実装せよ。

- 3.1 過去実験のディレクトリ構造の中で、tR.csvや、その他typeになりうるFBC.csv以外のデータ(またあなたに提示したことのない測定結果csvもある)が記載されていないが、ここにあるのはただの例という理解で良い？
- polarsって圧縮されててもそのまま読める？もし読めて、処理に問題ないなら解凍はしなくても良い。
- 複数の result_history に同じ `result.0001` が存在するため、Epoch の値は**「result_history のラベル + epoch 番号」**の文字列とする（例: `expA#0001`）。ラベルのデフォルトは result_history の**親ディレクトリ名**（=実験ログディレクトリ名）。とあるが、実験ログディレクトリの構造は{実験ログディレクトリ名}/Step{数字}/Loop{01等の二桁の数字}/の中にその実験のあるLoopのあるStepにおける実験データ群があり、result_historyはここにある。
- バッチ処理関連のスクリプトもscorelib直下に置くのが良い？ディレクトリなどで分けない方が良い？
- 最初のうち（データサーバとの連携等の仕様が固まるまで）はその最適化実験を実行するローカルに置いてあるデータのみで活用することも多いため、ダウンロードして活用する機能も、ローカルのデータをそのまま活用する機能もどちらも必要であることに留意。なんならそのハイブリッドも当然あり得る。
- ダウンロードに関しては仕様は定まっていないが、scpか共有マウントかで実装は大きく変わる？
- バッチサイズの規定は50でも良いが、マシンスペック等から推奨値(大きく問題がないなら50のままか、もっと増やしたり減らしたりなど)を推定したり、メモリが足りない、余っている等の警告を出すことはできる？ここは必須ではないがあれば便利そう。ちなみに最適化実験を行うのはubuntuOSが入っているのが一般的。バージョンはマシンにより、古いものもある。
- 出力データフレームのEpoch, History, EpochNoってなに？


- 実際にバッチ処理で適当に50バッチのときに何分くらいで終わるか知りたいがどうしたら良い？
- 今用意しているバッチ計算の時間計測について、同じファイルをコピーするような形で使っている？その場合、polars等がうまく勝手にキャッシュみたいなものを使っているおかげで早くなったりはしていない？
- また、 --history /data/expA/Step1/Loop01/result_historyこれはどこのファイルのこと？

python scripts/benchmark_batch.py --config config.jsonc --history /data/expA/Step1/Loop01/result_history --dvtbudget-coef dvtbudget_coef.jsonc --batch-sizes auto,10,25,50 --repeat 2 --max-threads 4


- scorelib/ ディレクトリの親（SVN checkout 先）とは？scorelibって最適化スクリプト達と同じSVNに入れる方針じゃなかったっけ？最適化スクリプトはkicOptという名前のディレクトリの中にまとまっているため、scorelibエンジンもここに置くのかと思っていた。
- 根本的部分について、compute_epoch_score()の引数について、最適化スクリプトからスコア計算するとき、configは当然すでに読まれていて、get_score関数内でもself.configのような形でアクセスできる。

turbo.py 自体が kicOpt にあるについて、kicOpt/optlibの中にあるが、今のままで大丈夫？

- バージョンをあげるときの0.3.0みたいな名前、変え忘れそう。どこを変えれば良いかどこかに書いてる？

- ここまでのスクリプトは社用PCに移して、StreamlitUI用のスクリプトはUI用のサーバなどに置く必要があり、スコア計算は"ユーザが現行GUI上から操作する最適化スクリプト"が動作する用のlinuxサーバに置かれる必要がある。どのファイルやディレクトリをどこに置く必要がある？確かscorelib\introspect.pyあたりのファイルはUI側のファイルと聞いていた気がするが、何をどこに置いて、何をインストールする必要があるのか上手く整理できる？
- 現在の理解は、UIはUIを起動するPCにエンジンとともに置く。エンジンは最適化スクリプトがまとまったSVNリポジトリの、kicOpt直下に置く。kicOpt/optlib/turbo.pyのget_score関数にブリッジを追加する。これで配置はあってる？テストディレクトリやその他は特に何もしなくてよい？
- サブプロセスでpython3.13あたりをminiforgeで起動、動作させるようにするにはその最適化実行するUbuntuサーバで何をどうすれば良い？現時点でminiforgeでnb37という仮想環境を作って、そこでpython3.7の最適化スクリプトを動作させている。


# CPU（モデル名・コア数。「CPU(s):」の行が論理コア数）
11th Gen Intel(R Core(TM) i5-11400 @2.60GHz

# メモリ
Mem: total 38G used 262M free 22G shared 1.4M buff/cache 16G available 38G
Swap: total 46G used 256K free 46G

# データ置き場のディスク種別（Type が nfs/cifs ならネットワーク越し = I/Oが遅い可能性大）
Filesystem /dev/sdb1 Type ext4 size 1.8T Used 1.7T Use% 97% Mounted on /home

# ディスクが HDD か SSD か（ROTA=1 なら回転ディスク=HDD）
sdb 1 1.8T WDC WD20EZAZ-00L

# polars が使っているスレッド数（= 計算並列度）
12

- WLのGroup分割をした際、それぞれのgroupに別々の重みをかけたいことがある。
- config_mini.jsoncにその例を示した。WLgroupWeightというkeyで、各WLgroupに自由な重みをかけることができる。
- また、これは辞書形式でなく一つの値であれば、すべてに同じ重みをかける操作もすることがある。
- WLgroup分割後であれば、ユーザが望む場合は重みをかける指示をOrderに追加することで、好きなタイミングで重みをかけられるようにすることはできる？
- また、WLgroupとは関係なく、算出している値に定数をかけたりしたいこともある。例えば正負反転のために-1をかけるなど。
- これを考慮すると、offset指示にadd以外の操作を追加することはできる？別にoffsetと分けても構わない。使いやすさを重視する。
- つまり、offsetに組み込むとしてもそうでないとしても、定数を足したりかけたり引いたりなど、そのような演算は一回ではなく複数できるようになっていた方が良いかもしれない。
- 更に、WLgroup分割の定義を書く際には、WLgroupDefinLogicalというkeyがTrueであれば今のままだが、Falseであれば反転した数値を記入できる機能も現行のスクリプトには存在するため、これも組み込むことはできる？つまり、0~119までWL番号があるとして、csvファイル上ではLogicalで記入されているが、分割はPhysicalで考えることで、それぞれの番号が119~0に対応することを意味する。
- これはエンジンはもちろん、UIでも適切な場所で設定できるようにしたい。


__relative__ を明示配置したパーツはユーザの並びを
    尊重して対象外。

WLgroupの重みってオフセットのオプションになってるけどこれが使いやすい？
WLgroup分割定義が増えたら、WLgroupに重みをかける専用の操作をエントリ追加の選択軸に入るのとどっちが使いやすい？
今の方法ならSTR等を分割したときも使えるとは思うし、今の直接入力も便利な場面はあると思う。



- 担当者と会話し、仕様が考えていたものと異なることが分かったことと、前提を直さなければいけない点があった。
- まず大きな点として、相対化をする際、OverrideのTrue, Falseで判断しようとしていたが、このOverrideでは判断できないことがわかった。
- 軸を選ぶ際は、tests\data\result_tmp_mini\map_dataName.csvのデータネームをユーザが選ぶ形で指定することになる。例えば0,reference_param_read_level_1と1,evaluation_param_read_level_1という要素があったとき、分母としてreference_param_read_level_1、分子としてevaluation_param_read_level_1を選ぶなどの操作が必要になる。
- 一方で、このデータネームはユーザが最適化実験前に自由に決められるものとなっており、ルールが定まっていない。
- さらに、データネームをつけることを義務付けるルールもまだ整備されていないとのこと。
- また、将来的には実験設定から出力csvにどんな要素があり得るかを事前に知る機能を追加できるが、直近ではその機能がないとのこと。
- そこから、どのように集計設定をするべき？
- 少なくとも事前テストをすることは難しいと思うがどうか。
- 事前に情報が取得できた場合と、できない場合で集計のでき方は変わって、できない場合はMeasure列の数値で行うしかない？
- 現時点では確定していないが、もしかしたら出力されるファイル(typeとして選べるもの)もわからない場合がある。
- この前提に立つとどういう実装、修正をするべき？実装する前に提案して。
- また、最低でもこの情報が確定しないといけないというものや、取得できるかどうかがわからないといけないものがあれば担当者に確認するので教えて。


- あなたの理解に勘違いがありそうなので訂正する。
- dataNameは「GUIにとって未知」なだけで、「設定を書く人間にとって未知」ではない。について、一部は正しいが完全には正しくない。
- そもそもの流れを説明すると、パラメータ測定のフロー（指定のパラメータを書いたり読んだり消したり。これがProgram, Read, Eraseに対応している）をユーザがVBAを使って設計する。
- この設定によって測定の順番であるMeasureが決まるわけだが、そもそもこのフローを作るデータネームを付けない場合があるとのこと。
- つまり、文字列でいれることすらできない場合があるということになる。
- 自由記述も可能にすること自体は自由度向上の観点から否定しない。
- 実行時に不一致なら「'reference_param_read_level_1' が見つかりません。実データに存在するDataName: [...]」と候補列挙つきエラーで即座に直せるようにするとあるが、一回の測定には長くて20分程度かかるものもあるため、実データが得られるのに時間がかかる。
- 最低でもMeasureの数だけはスコア設計前に取得できるよう依頼するが、これ以上の情報がいつ得られるようになるかはわからない。
- 一方で、測定フロー自体は事前に存在することから、少なくとも解析スクリプトが作成されればいつかは手に入るようになるという認識。
- ユーザがフローを設計した人間であれば、どのMeasure番号がどの測定に当たるかは知っていることから、Measure番号であればひとまず相対化や、フィルタリング等の集計指示の情報として使用できるのではないかと考えた。
- Measure→DataNameの対応は dataName_{type}.csv にある通り (InBatchEpoch, Board, Chip, Block) ごとに解決されるもので、Measure番号は実験設定が変われば変わる不安定な識別子です。とあるが、ここは認識を合わせたい。
- 実験設定が変われば変わるというのは？ラベルがない場合があり、対応させられる情報がないのであればMeasure軸を使うしかないし、実験設定（測定フロー）が変われば同じスコアが使えないのはもう仕方ないかと判断したがどうか。

-  担当者に確認する前に確認事項をこちらで認識すり合わせをしたい。
1. **【実装前必須】同一実験内で Measure→測定内容の対応は
   全 (InBatchEpoch, Board, Chip, Block) で同一か**。同じフローが全ボード・全チップで走る運用か。
   バッチ(複数epoch)でも全 epoch 同一フローか。
   (dataName_{type}.csv 等のスキーマは (InBatchEpoch, Board, Chip, Block, Measure) キーなので、
   理論上はボードごとに違う対応を表現できてしまう。崩れる運用があるなら Measure 番号指定は成立しない)
   について、これはどういう意味？あるBoardではあるフローが走らないということがあり得るか？ということ？この場合、何があってMeasure番号指定が成立しなくなる？

3. ラベル・dataName 無し実験での出力: parameterLabel_{type}.csv / dataName_{type}.csv /
   map_*.csv は欠落するのか、空で出るのか。{type}.csv 単体で成立する形式か。
   について、これはどうとでもできると思う。どうしてほしいとかがある？それともそれが確定したら実装の仕方が確定するから、という意味？


typeファイルによっては、すでにある程度集計された後に出力されるファイルもあり、そもそもMeasureがないファイルもある。
- 理想的には何が欲しい？
1. 今回の実験と同じ形式（typeとして指定できるすべてのファイルやMeasureやデータネーム、チップやBoardの数などがすべてそろっており、測定値のみダミーのもの）のダミーファイルがある場合
2. ダミーファイルはないが、その測定で得られるMeasure情報やデータネーム等の情報が整理されている。
3. チップやボードの数がわかっている
4. 出力されるtype候補がわかっている
などいろいろあるが、可能性と、あるとより良い順位などある？
- ダミーが渡されたときとないとき、情報があるかないかなどで、テスト計算ができるか、要素の候補や数値候補が選択できる（例えばデータネームわかっていればデータネームを選べるなど）などの可否がわかると思うが、それによってＵＩの挙動を変えることはできる？候補表示やそもそもテストできないという文言とともにテストボタン出さないとかいろいろ。


担当者から相談が来た。
- 例えばあるBlock0ではMeasureを2回行って、Block1ではMeasureを3回、つまりMeasure0,1は同じだが、2だけBlock1に特別で行うような測定をした場合、スコア計算側では困るか？と聞かれた。
- 少なくとも測定結果ファイルを出力する側である担当者としては配列にずれがあるため、存在しない測定のところには負の値かNaNを入れてみようかと思ったがそれは困るか？とも聞かれた

- 担当者とさらに相談した。
- まず前から決まっているように、現在はサンプルとなるファイルを直接渡すことでUI上で候補を出しているが、実際には測定データがスコア設計段階は測定前のため存在しない件。
- まずこの担当者は一般ユーザが測定フローを定義できるVBAを作成している。
- この測定フローというのは、どのパラメータでデータを消去し、どのパラメータでデータを書き込み、どのパラメータでFBC等を読みだすか、といったものを定義するものである。
- このフローによって、Blockごとに測定を変えるなどができるようになっている。
- このフローを解析することによって、少なくともどのファイルが出力されるか、Measure, DataNameの数や内容は取得できるか検討するとのこと。
- 少なくともどのファイルが出るかとMeasureの数については、これがわからないとまともに使われるUIにならないため、これについては何とかするとの回答を得た。
- これに合わせて、各ファイルにどんな軸が出力されるのかも情報を出力できるようにするとのこと。
- 軸の中の要素内容については、Boardの数やChipの数などはフローが知りえないものとのことなので、他から取得する必要があるが、これは別の場所から取得できる可能性がある。 
- ここで、dataNameについて、先日伝えたように将来的には義務付け、もしくは自動で付与されるようになるが、現時点ではユーザが入れるかどうかになる。
- 最初期はこの担当者が付与したフローを渡すため、dataNameはあるし、後々はdataNameが付与されていないフローを出力しようとするとエラーとすることも検討しているとのこと。
- 一方で、このdataName名付与をユーザに強制すると負荷となるため、基本的には自動化方針とのこと。
- ここまでのことを踏まえると、相対化やなんらかのラベルごとの演算やフィルタリングをしたいとき、基本的にはdataNameで選べると良いが、dataNameがない場合があることを考慮するとMeasure番号で指定することをベースにする必要があると考える。
- dataNameがある際は、ユーザがdataName列を選んだ場合、内部処理的にはdataNameに対応するMeasure番号で抽出すれば良い？
- ここまで考えたとき、データの入力方法はどうすると良い？世代情報等はこれまで同様取得するとして、取りうるtypeやその軸情報が得られた場合と得られなかった場合の処理をまず考えたい。
- また、得られた場合は、どういう形で入力されると良い？仮のcsvを作ってもらっても良いかもしれないが、各軸の要素数までは要素が得られない場合もあると思う。
- これらのときに、テストの可否なども含めて、どういう計算、どういうUIにすべきか提案せよ。

- 確認。UIにこれまでmeasure番号は表示してなかったと思うが、今後はどうする？
- さっきも書いたように、担当者が取得できる内容以外に、別の場所から取得できる情報もある。これと、カタログjsoncをどう関係させる？
- 以下は確認事項について。
2. 事前取得できる「Measure の数」は type(FBC, tR, ...)ごとか全体で1つか。番号は0始まり連番か、欠番はありうるか。について、type全体で一つ。そもそもMeasure列がないものはある。番号は0始まりで欠番はない。
3. ラベル・dataName 無し実験での出力形式。について、欠落にしてほしいとは頼めるはず。依頼すべき事項は、さっきのカタログjsonの話なども含めて依頼事項としてまとめよ。
4. type の事前把握: フローから「どの csv が出力されるか」は機械的に決まるか。「type もわからない」ケースの実態は何か。これはさっき回答した通りだが、まだ確認すべきことはあるか？
4b. **Measure 列の無い集計済み type** について: そのファイルで基準パラ/評価パラを区別する列は何か(存在するのか)。Measureがないというのはすでに測定データを何らかの方法で集計しているということ。相対化自体はせず、似たようなことをしたいとしても、Chip番号等で割り算や引き算等ができれば良いはず。
5b. **dataName と Measure は 1:1 か**。基本的にはないが、ユーザが特殊な測定フローを定義したとき、例えばあるdataNameに属するループ内で複数の測定をしたときなど、同じdataNameで別Measureが存在しうるらしい。
5. フロー解析スクリプトの実現性: フローファイル形式は固定・解析可能か。解析結果の受け渡し形式(2節のカタログ供給ファイル)をこちらから提案してよいか。これは提案して良いはずだが、VBAからの出力のためcsvなどになってしまう可能性はある。


- 社内で投稿する社内論文のようなものを作成する依頼を受けている。
- 社内論文は、本当に軽い論文形式のものから、例えば休憩時間や深夜時間も考慮した勤務時間の計算方法から考えるプログラミングの書き方のようなものまで存在している。
- 今回行っているスコア作成UIとそのエンジンについて、その作り方や考え方などはこの社内論文になりえるだろうか？
- それよりもこのテーマではなく、実際に行っている最適化実験に関することの方が良いだろうか？
- 分量についてはwordで100ページを超えるようなものもあるが、主に参考文献や画像込みで10ページ前後良いとされることが多い。

- まずは担当者との会話、合意内容について説明する。
- まず、測定フローから、今のresult_tmpに当たるような、最適化の1epochで出力されるデータをcsvの形で、測定値（FBCやtRなど）だけダミー値として出力できるとのこと。
- しかし、測定フロー設定側では知れない情報があり、それがBoard,Chipである。
- これは測定フローから得られるファイルにはboard,chipともに1つしかない状態で出力するとのこと。
- このBoard, Chipだけ、スコアUI側で最初に「この実験ではBoardの数はいくつ、Chipの数はBoardごとにいくつで実験する想定か」を入力し、その情報をもとに受け取ったデータを整形することで、テスト可能なデータを内部で保持し、UIを動作させれば良いのではないかと考えている。
- 相対化の指示を出す際、基本的にMeasure番号を基準として、それに対応したデータネームも存在する場合はそれも併せて表示する仕様としたい。
- また、大量のMeasureがあるときにユーザがミスをする可能性があるため、データネームのつけかたにルールを定め、そのルールに則って相対化ペアの候補を出してほしいとのこと。
- ここでいうルールとは例えば、reference_param_read_level_1, evaluation_param_read_level_1, reference_param_read_level_2, evaluation_param_read_level_2のように、reference, evaluationでペアを作れるような名前のイメージ。
- ここまでで確認事項の更新と、残りの事項、追加の事項はあるか？

1. 【最重要】ダミー一式のファイル範囲: {type}.csv だけか、parameterLabel_* / dataName_* / map_*.csv も含む完全一式か。について、完全に一式とのこと。
2. ダミーのキー構造の忠実度: 測定値以外(WL, STR, State, Measure, Block の行構成)は本番と完全一致か。特に Block はフロー側が知っているので実数で出るのか(6.5節ではshape_catalog側に置いていた)。「無いものは出力しない」原則(4.6節)はダミーにも適用されるか。について、Blockも含めて実際の数が出る。ないものは出力しないとできるはず。
3. ボード別フローの表現: 4.6節(i)で「BlockごとにMeasure回数が違ってよい」と合意しましたが、Boardごとに走る測定が違う実験はBoard1つのダミーでは表現できません。その場合ダミーはどう出るのか(全Boardの和集合?そもそもボード別フローは無い?)。Board複製方式は「全Board同一測定」前提なので、ここが崩れると被覆情報表示(プラン8)の設計時サポートが実データ後専用に格下げになります。について、測定フローはBoardやChipに関わる設定はしないとのこと。するようになったら仕様が変わる。
4. 命名ルールの正式化: reference_/evaluation_ ペアのルールを仕様として文書化してもらう必要があります(7節の依頼2「採番規約の明文化」と同格)。具体的には: プレフィックスは reference_/evaluation_ 固定か、reference_X ↔ evaluation_X の残り部分完全一致でペアとみなす規則でよいか、大文字小文字の扱い。について、完全には決まっていないため、今暫定でも作らない方がいいなら一旦Measure番号（＋データネーム）だけの形式でも良い。
5. 1:多dataNameとペアルールの交差: reference_X が複数Measureに付くケース(ループ内測定、5b回答で存在確認済み)でペア候補はどう出すか。v1では「ペア候補提示は1:1のときだけ、1:多は手動選択」への縮退を推奨します。について、全く同じ名前ではなく何かしらのprefixをつけるなどがある。これも4と合わせて確定してからでも良い。
6. Board/Chip数入力の粒度:について、Boardごとに異なるChip数を許す。
7. カタログ形式の扱いの最終判断: ダミー一式を唯一の事前情報経路として6.2〜6.5節の設計(jsonc形式・csv群・マージ規則)を廃棄するか、将来のフォールバックとして仕様だけ残すか。私の推奨は廃棄(ノートに経緯だけ残す) — ダミーが確約されるなら二重経路は保守コストだけ払うことになります。ただし担当者の確約の固さ次第です。について、あなたの推奨で良い。
8. ペア候補提示の位置づけ: ルールに合う名前が無い/ルール外でも手動選択は常に可能(候補提示はあくまで支援、強制しない)でよいか。常に手動でMeasure番号を選択する方式はあった方が良いと思われる。
- 雛形の相対化プリセット(DEFAULT_RELATIVE)の扱い: 廃止か、ペア候補ルールで自動プリセット(reference→分母/evaluation→分子)に進化させるか。について、今ってどうなってるんだっけ？一旦最初のペアがあればそれを自動で入れても良いと思うが。
- filter の is_in スキーマ表現について、フィルターは複数項目選べた方が良いと思う。
- filter で dataName 選択したときの保存形(Measure解決に統一するか)について、基本的にMeasure（＋データネーム）を基準にするのが良いと考えている。
- validate モードのCLI形態(ダミーでテスト計算が通るため重要度は下がるが、誤再利用警告のため存続推奨)について、validateモードってなんだっけ？
- 担当者小確認 4(a)〜(d) — ただし4(b)(map要素の正)はダミーにmap_*が含まれるなら解消について、以下で回答。
- 4(a) ファイル命名規約({type}.csv 等)は新typeでも維持されるかについて、維持するように要求することが可能なはず。
- 4(b) map_*.csv の要素候補の正はどこか・カタログの axis_values に含められるかについて、意味はわかっていないが、解決しているか？
- 4(c) type名はシステム固定かユーザ定義かについて、これは厳密にいうと測定データはnumpy配列であり、これをデータフレームの形に整形するスクリプトを作っている担当者がいる。もともとFBCも測定の返り値では別の名前であることから、ユーザ定義に当たると思う。
- 4(d) バッチ(複数epoch)でカタログは全epoch共通1つでよいかについて、カタログとはなに？

2. 4(c) から派生する新しい注意点:について、FBC名維持の方向で良い。
3. 雛形プリセット: 上記のとおり「v1はプリセット無し、第2弾でペア自動セット」で良いか。について、それでよい。
4. ダミー自体は今あるものを整形（Board/Chipを0のみにする）することでダミーと見なせるからそれで試したりもできると思う


これまでのドキュメント反映漏れはない？
バージョンも大丈夫？
全 Board 共通なら数1つ（例: 4）、Board ごとに違うならカンマ区切りで Board 数ぶん　ぶんが日本語なのはなに？
世代情報はいる？

スコアだけ変えたいこともあると思う。ちょっとした変更ならダミーファイルも実測値もいらず、テスト計算もせずに変更したいこともあると思う。そういう時にスコアを定義したconfigだけをアップロードし、例えばWLgroupの定義やスコア式だけを変えたりはできる？

- 今は新しい機能をmainブランチにそのまま実装して、タグ付けもしていない。
- そもそもソフトウェア開発の観点から見たとき、これはどういう運用にすべき？
- ブランチ分け、CI/CD等も含めて、正しい知識がないため推奨される開発の仕方を説明して。


- pre-push, gitattributes, CHANGELOG, test.yml, hooksというディレクトリどれも見たことない。それぞれどういうもの？さっきの運用の仕方も含めて、自分用のメモに残したい。どこかに書いてある？

- 実際の運用は社内専用のGitLabで動く。CI機能があることは知っているが同じ運用方法で大丈夫？それとも社内環境で実際に確かめた方が良い？

- これまでいろんな機能を追加したり、機能改変で消したものもあると思う。300件近いテストがあるとのことだがすべてのテストがいる？意味なく消せとは言わないが、不要なものはない？

- 今開発とかテストはwindows環境上で実行してて、.venv/から始まるコマンドでいろいろ実行していると思う。
- 社内開発環境ではUbuntuサーバに今のリポジトリを置いて、実行サーバ（Ubuntu）に必要なものを置いたり、ＳＶＮにエンジンを置いたりする。
- ＳＶＮ側は以前も言ったようにminiforgeによる仮想環境でpython3.7が実行され、ＵＩはどうするかまだ決まっていないがdocker等も使えるとのこと。
- 実際のところ、社内開発環境ではどうやってスクリプトを実行して、UI実行サーバではUIを実行するために何を置いてどのように実行すべき？
- ドキュメントとかはUI建てるサーバなどには置きたくない。
- 具体的に開発場所以外には何を置くかはREADMEあたりに今のところ書いてあると思うが、上記まで含めて書いてある？
- 環境等について疑問があれば質問して。

1. UIサーバへの配布経路: UIサーバから社内 GitLab に到達できます(clone/pull できる)か? それとも zip や scp での手渡しになりますか(更新手順の書き方が変わります)について、到達できるはず。
2. 利用者のアクセスとポート: 利用者の PC ブラウザから UIサーバの 8501 番(任意のポート)に届く見込みですか? また Streamlit 自体には認証機能が無いのですが、社内 NW 内なら認証なしで問題ない環境ですかについて、ここは確認が必要。今はわからない。
3. Docker の制約: UIサーバ上で docker build できますか、それとも社内レジストリから pull だけですか? base イメージ(python:3.13)の取得可否は GitLab CI と同じ閉域問題がありますについて、dockerについて詳しくないためよくわからない、これについても確認が必要。
4. pip の到達性: 開発 Ubuntu / UIサーバから PyPI(または社内ミラー)に届きますか? ミラーなら URL 指定が要りますについて、普段から普通にpip installなどしているが、それがミラーなのかよく知らない。
5. 開発の主戦場: 今後の開発(コード編集・テスト)は Ubuntu サーバ上(SSH)で行い、Windows PC は使わなくなる想定ですか? それとも Windows で開発して push し、Ubuntu は実行・検証だけですか(README のコマンド表記をどちら主体にするか決めたい)について、Ubuntuメインとした方が良いと思う。windowsの場合はサブとしてどこかに残っていれば助かるかも。


- UIについて、いくつか要望。
- 全 Board 共通なら数1つ（例: 4）、Board ごとに違うならカンマ区切りで Board 数ぶん　ぶんが日本語なのはなに？
- アップロード機能が足りていない。
- ダミー一式や、optimization設定jsonc のパス（任意）あたりのところ、パスをいちいちコピーして貼り付けるのはめんどくさいと思うが、アップロード機能はつけられる？

- 上級者向けという文言はいらないと思う。
- これはこうしてほしいというよりも感覚論だが、アップロード形式とパス形式の一つ目が同じ項目、パス形式の残りが個別ファイルと同じ形式というのも結局わかりづらい気がするがどうだろうか？
- また、実際のところサンプルのconfigを読まない、つまり全くのサンプルなし状態からスコア設計することはかなり稀で、普通は既存スコアが書かれたconfigを読み込んで、そこから使いたいものを取捨選択したり調整したりするのが普通だと思う。
- この観点から考えてより良い画面構成はないだろうか？難しければ一旦今のままでも良い

- 編集の出発点、普段は既存スコアの設定（score.jsonc / optimization設定jsonc）から始めて、取捨選択・調整します。などと書いてあるが、普段は～とかをユーザが読んで意味不明だと思わないか？普段ってなんだ？
- 値の候補表示とテスト計算に使う。これもこんなでかでかと書くようなことじゃない。小さい説明には書いてあっても良いが情報量が多すぎる。
- 何度修正しても伝わっていないようだが、パスの入力場所がおかしくないか？普通に考えてみてほしい。ユーザがいざこのUIを使おうとしたとき、ファイル一式をアップロードし、dVtBudgetの係数を使おうと思ってプルダウン開いたとしたらそこにまたパス指定の枠があったらどうしていいか迷うと思わないか？

- 開発者オプションのダミーを選択したときだけ追加ファイルの入力欄がなく見えるが、これで正しい？

- ここからは全く別の話をする。
- 実際にこのUIを運用しはじめたとき、複数ユーザが使うことがある。
- 複数ユーザが同時にこのUIのサイトにアクセスして操作するとおかしくなると認識しているがあってる？
- その場合、対策する方法はある？
- この辺りの知識が疎いため、正確な情報を詳しく教えて。

- zip 展開・ダミー展開・アップロードの一時ディレクトリ: 毎回ユニークな temp ディレクトリを作るので衝突しません。ただし溜まる一方なので、サーバでは定期削除(cron や systemd-tmpfiles で /tmp 掃除)が要ります。そうなの？いまローカルで何回もテストしているが溜まってる？どうやって消す？消すのは実際に運用が始まったときの場合についても教えて。
- 必要なら定石はリバースプロキシ(nginx 等)を前段に置いて Basic 認証や社内 SSO をかける方法で、アプリ側の変更は不要。これは全く専門外なのでよくわかっていない。具体的になにをするもの？このリポジトリでなにかする？
- 2. 起動時に名前を入力してもらい、~/.scorelib_drafts/<名前>.jsonc に分離。方針はこれで良いが、認証がついたらそこから情報を取れるようになるの？


- KLDは一般的にBoard/Chipで平均を取り、np.log(np.maximum(np.fabs(x), 1.0e-6)/1)という演算をしたあと、規定の重みをかけて、SGWLD軸で総和を取るような計算をする。
- 規定の重みとは、全体に0.1をかけることもあれば、SGWLD方向に別の重みをかけることもある。
- dVthSGWLDではBoard/Chip/Blockで平均を取り、SGWLD軸で絶対値を取ってから総和を取るということが一般的。
- このような計算はできる？
- また、vthskipという設定があった場合、その設定の中で指定されたepoch数に到達するまではKLDとdVthSGWLDが測定されないことがある。
- このSkipされるepochの間はダミー値を計算して返すようにしたい。
- ここでいうダミー値とは、これらのSGWLD軸に、設定に記述されたダミー値が入ったうえで上記計算をするイメージ。
- よくあるダミー値がKLDは0, dVthSGWLDは1なので、恐らく計算後にKLDは0, dVthSGWLDは8になると思う。
- このとき、そのepochまではこれらのファイルが出力されないため、スコアパーツとして存在するのにこれらのファイルがない場合はダミー値で計算するようにするとかになる？
- このような計算をするようにすることはできる？
- UI上ではファイルがある前提になると思うのでダミー値のことは気にしなくても良いかもしれないが、実際の最適化のときにこういう動作をするようにできる？

- もともとWLgroup定義がoptimization階層に書いてあるconfigを読んだとき、それを出力したconfigはgroupDefsの方に情報が書かれると思うが、これをこのまま使うと元のGroup定義と重複しないか？
- pre-push でvenvがないときスキップしているがこれで良い？CIに任せるのと、きちんと止めるのとどっちが良いだろうか

- ブリッジでサブプロセスを実行するとき一度configを保存していると思う。ここでエラーが出た。
- エラーの内容としてはSeriesが含まれているからdumpに失敗するというもの。
- 実際にKLDweightなどは各SGWLDの名前ごとに重みが入ったSeriesになっており、そのほかにもWLgroupWeightなども同様である。
- これは、config読み込みを作った担当者が、スコア計算の際に計算しやすくするために処理しているものだと思われる。
- 旧バージョンのスコアを使う場合はこのSeriesを使っているし、このconfig読み込みにはconfigに記載されていない内容の自動補完なども備わっている。
- どういう対処をするのが良さそう？もしかしたらスコア計算に関わる部分は追記されていないかもしれないという補足はある。

- また、そもそもUIでのテストやスコア計算を実際に行いたいとき、そもそもbudgetの計算係数はそのディレクトリから自動で読むようにはできないか？基本はSVNリポジトリに存在しているので、

- 社内からアクセスできる？今のUIはどうやってみんなアクセスしてる？

Board, Chipの情報がないということはBoardごとに違う測定はしない？

totalDVthの計算について、SGSBとか省いているのが今だが、これもユーザに自由に選べるようにしておく？

map_SGWLD.csvはstandard_conditionの内容に合わせて作られるはずだが、
これはフローやモチーフに合わせてちゃんとできている？
skipのときにもmapが作られて、それをもとにダミー値計算をする（ダミー値はSGWLDで和を取る演算などははいる。）

- 計算typeになりえるものはFBC, tRなどの他にもある。ひとまず中身がわかっているものと、mapの例をtests/data/result_tmp_miniに追加した。
- これらも問題なく計算できるか？


- 今ある設計書は設計側の私との会話の内容等も記述されており、ユーザに見せるものにはなっていないと思う。StreamlitUIの使い方等をユーザ向けに説明するファイルを作成したいのだが、READMEがそれにあたる？画像等を差し込む必要がある場合は、そちらで差し込むのが難しければ何かしらの文言でファイル上などにこういう画像を置くと指示を残しておいてくれればこちらでスクリーンショット等をとって画像を置く。

- また、これまでに作ってきた各mdファイルにどういう内容が書いてあるか、無意味な重複があったり、まとめたりできるものがあるかを適切なファイルにまとめよ。README.mdはREADMEとしての役割を果たせる内容になっているかなども確認せよ。なお、私がメモ代わりに使っているscore_gui.mdは無視して良い。


データフレーム返してほしい
UI横向き
axis_resolveのJOIN_KEYS　この軸が絶対あるとは限らないが、これはなに？
スコアを名前で保存したくなることあるかな？
